## SA1134

<table>
<tr>
  <td>TypeName</td>
  <td>SA1134AttributesMustNotShareLine</td>
</tr>
<tr>
  <td>CheckId</td>
  <td>SA1134</td>
</tr>
<tr>
  <td> атегори€</td>
  <td>ѕравила удобочитаемости</td>
</tr>
</table>

:memo: Ёто правило €вл€етс€ новым дл€ **StyleCop Analyzers**, и его не было в **StyleCop Classic**.

## ѕричина

јтрибут помещен в ту же строку кода, что и другой атрибут или элемент.

## ќписание правила

Ќарушение этого правила происходит, когда в одной строке кода размещаютс€ два атрибута, или атрибут размещаетс€ в одной строке кода с другим элементом.

Ќапример, следующий код приведет к нарушению этого правила:

```csharp
[ExportCodeFixProvider(LanguageNames.CSharp, Name =
nameof(MyCodeFixProvider))][Shared]
public class MyCodeFixProvider : CodeFixProvider
{
}
```

—ледующий код не приведет к каким-либо нарушени€м:

```csharp
[ExportCodeFixProvider(LanguageNames.CSharp, Name = nameof(MyCodeFixProvider))]
[Shared]
public class MyCodeFixProvider : CodeFixProvider
{
}
```

### »сключени€

Ќарушение этого правила не произойдет дл€ атрибутов, размещенных у параметров или параметров типа.

Ќапример, следующий код не приведет к нарушению:

```csharp
public class MyClass<[Foo][Bar] T>
{
	public T MyMethod([In][MarshalAs(UnmanagedType.LPWStr)] string value)
	{
	    ...
	}
}
```

##  ак исправить нарушени€

„тобы исправить нарушение этого правила, поместите каждый атрибут в свою собственную строку.

##  ак подавить нарушени€

```csharp
#pragma warning disable SA1134 // јтрибуты не должны совместно использовать одну строку
[ExportCodeFixProvider(LanguageNames.CSharp, Name = nameof(MyCodeFixProvider))][Shared]
#pragma warning restore SA1134 // јтрибуты не должны совместно использовать одну строку
public class MyCodeFixProvider : CodeFixProvider
{
}
```