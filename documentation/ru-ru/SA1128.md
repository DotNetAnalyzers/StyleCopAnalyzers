## SA1128

<table>
<tr>
  <td>TypeName</td>
  <td>SA1128ConstructorInitializerMustBeOnOwnLine</td>
</tr>
<tr>
  <td>CheckId</td>
  <td>SA1128</td>
</tr>
<tr>
  <td> атегори€</td>
  <td>ѕравила удобочитаемости</td>
</tr>
</table>

:memo: Ёто правило €вл€етс€ новым дл€ **StyleCop Analyzers**, и его не было в **StyleCop Classic**.

## ѕричина

»нициализатор конструктора находитс€ в той же строке, что и объ€вление конструктора, в файле кода C#.

## ќписание правила

Ќарушение этого правила происходит, когда код содержит инициализатор конструктора, который частично или полностью находитс€ в той же строке, что и объ€вление конструктора.

Ќапример, следующий код приведет к нарушению этого правила:

```csharp
public class TypeName
{
    public TypeName() : this(0)
    {
    }

    public TypeName(int value)
    {
    }
}
```

—имвол `:` (двоеточие) рассматриваетс€ как часть инициализатора и должен находитьс€ в той же строке, что и ключевое слово `base` или `this`.

—ледующий код также будет нарушать это правило:

```csharp
public class TypeName
{
	public TypeName() :
		base()
	{
	}
}
```

—ледующий код не приведет к каким-либо нарушени€м:

```csharp
public class TypeName
{
    public TypeName()
        : this(0)
    {
    }

    public TypeName(int value)
    {
    }
}
```

##  ак исправить нарушени€

„тобы исправить нарушение этого правила, перенесите инициализатор конструктора, включа€ символ `:`, в отдельную строку.

##  ак подавить нарушени€

```csharp
public class TypeName
{
#pragma warning disable SA1128 // »нициализаторы конструктора должны находитьс€ в отдельной строке
    public TypeName() : this(0)
#pragma warning restore SA1128 // »нициализаторы конструктора должны находитьс€ в отдельной строке
    {
    }

    public TypeName(int value)
    {
    }
}
```