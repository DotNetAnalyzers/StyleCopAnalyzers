## SA1136

<table>
<tr>
  <td>TypeName</td>
  <td>SA1136EnumValuesShouldBeOnSeparateLines</td>
</tr>
<tr>
  <td>CheckId</td>
  <td>SA1136</td>
</tr>
<tr>
  <td> атегори€</td>
  <td>ѕравила удобочитаемости</td>
</tr>
</table>

:memo: Ёто правило €вл€етс€ новым дл€ **StyleCop Analyzers**, и его не было в **StyleCop Classic**.

## ѕричина

Ќесколько значений перечислени€ размещены в одной строке кода.

## ќписание правила

Ќарушение этого правила происходит, когда два или более значений перечислени€ помещаютс€ в одну и ту же строку кода.

Ќапример, следующий код приведет к нарушению этого правила:

```csharp
public enum ExampleEnum
{
    FirstValue, SecondValue
}
```

—ледующий код не приводит к каким-либо нарушени€м:

```csharp
public enum ExampleEnum
{
    FirstValue,
    SecondValue
}
```

##  ак исправить нарушени€

„тобы исправить нарушение этого правила, поместите каждое значение перечислени€ в отдельную строку.

##  ак подавить нарушени€

```csharp
#pragma warning disable SA1136 // «начени€ перечислений должны располагатьс€ в отдельных строках
public enum ExampleEnum
{
    FirstValue, SecondValue
}
#pragma warning restore SA1136 // «начени€ перечислений должны быть в отдельных строках
```